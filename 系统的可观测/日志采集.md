1、WAL(Write Ahead Log)日志是一个什么机制。为啥先预写日志可以保证数据的可靠性
2、LSM Tree(Log Structured merge Tree)  是什么 和 WAL 有什么区别
3、WAL 日志机制，底层磁盘，那如何保证 日志的大小？会持续膨胀？
4、 LSM-Tree 的MemTable 和SSTable  是什么 ？如何理解，如何实现？
5、 java 有跳跃表的实现类？
6、ConcurrentSkipListMap 和currentHashMap 有啥区别？分别使用什么场景？
7、LSM 的合并 sstable 流程是如何？ 合并策略详解下。如果是多层方式，是如何的？
8、LSM 的 key 如何设计保持有序呢？
9、为啥redis  不采用LSM?
10、如果是，可观测平台的数据存储，采用 LSM 是否更加合适？
11、阿里云的 OTS  是设么特性？
12、为啥OTS能支持：支持宽表模型（Wide Column）、时序模型（TSDB）、搜索索引、多元索引等。宽表模型 是因为可以追加到key? 时序模型是有key 可以是有序吗？  那索引和多元索引如何实现？
13、独立的二级索引存储，基于倒排 + 列式存储构建，详细解析这原理